using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Assigner : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*if (name == "ContinuePanel")
            GameManager.Instance.restartCheckPointPanel = gameObject;
        else if(name == "Lives Text (TMP)")
        {
            GameManager.Instance.livesText = GetComponent<TMPro.TMP_Text>();
        }*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
